from setuptools import setup, find_packages

setup(
    name="helm",
    version="0.0.0",
    packages=find_packages(
        include=[
            "helm",
        ]
    ),
    description="",
    install_requires=[],
)
